{
  "name": "backend",
  "version": "1.0.0",
  "description": "resolvet customer support ticket backend",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon server.js",
    "start": "node server.js",
    "full:run": "npm install --omit=dev && npx prisma generate && npx prisma migrate deploy && node prisma/seed-csv.js && node upload-attachments.js && node server.js",
    "full:setup": "npm install --omit=dev && npx prisma generate && npx prisma migrate deploy",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "prisma:studio": "prisma studio",
    "prisma:seed": "node prisma/seed-csv.js",
    "prisma:seed-original": "node prisma/seed.js",
    "prisma:seed-csv": "node prisma/seed-csv.js",
    "prisma:reset": "prisma migrate reset && node prisma/seed-csv.js",
    "upload:attachments": "node upload-attachments.js",
    "setup:complete": "node setup-with-attachments.js"
  },
  "author": "resolvet",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^6.16.0",
    "bcryptjs": "^3.0.2",
    "cloudinary": "^2.7.0",
    "compression": "^1.8.1",
    "cors": "^2.8.5",
    "dotenv": "^17.2.2",
    "express": "^4.21.2",
    "express-oas-generator": "^1.0.48",
    "helmet": "^8.1.0",
    "joi": "^18.0.1",
    "jsonwebtoken": "^9.0.2",
    "mime-types": "^2.1.35",
    "morgan": "^1.10.1",
    "multer": "^2.0.2",
    "nodemailer": "^7.0.6",
    "redoc-express": "^2.1.0",
    "swagger-ui-express": "^5.0.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.10",
    "prisma": "^6.16.0"
  }
}
